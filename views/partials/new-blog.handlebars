<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/blog/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.name}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
</div>