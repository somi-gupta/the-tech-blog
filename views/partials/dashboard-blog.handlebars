<div class="card">
    <div class="card-header">
        <h4><a class="link" href="/dashboard/edit/{{id}}">{{title}}</a>
            <a style="font-size:smaller">{{user.name}}, {{format_date created_at}}</a>
</h4>
    </div>
</div>